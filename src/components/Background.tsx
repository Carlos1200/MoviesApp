import React from 'react';
import {View} from 'react-native';
import {backgroundStyles} from '../theme/backgroundTheme';

export const Background = () => {
  return <View style={backgroundStyles.container} />;
};
